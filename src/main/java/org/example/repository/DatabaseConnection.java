package org.example.repository;

/*
The gravity of adopting Hibernate cannot be overstated.
It represents a paradigm shift in database interaction,
offering numerous advantages such as transparent persistence
and enhanced abstraction.
* */
/*public class DatabaseConnection {

    public static Connection getConnection() throws SQLException{
        Connection connection = null;
        try {
            String url = "jdbc:mysql://root@localhost:3306/wumpus";
            String username = "user";
            String password = "password";
            connection = DriverManager.getConnection(url, username, password);
            System.out.println("Connected to the database!");
        } catch (SQLException e) {
            System.err.println("Connection error: " + e.getMessage());
        }
        return connection;
    }
} */
